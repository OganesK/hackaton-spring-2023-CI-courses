import { makeStyles } from '@material-ui/core/styles';

const useClasses = makeStyles(() => ({
  createGroupButton: {
    padding: '9px 27px',
    cursor: 'pointer',
    borderRadius: 4,
    width: '100%',
  },
}));

export default useClasses;
